---
title: 单调栈
date: 2025-06-15
tags: [数据结构]
---

先看例题，力扣[739. 每日温度](https://leetcode.cn/problems/daily-temperatures/)。

> 给定一个整数数组 `temperatures` ，表示每天的温度，返回一个数组 `answer` ，其中 `answer[i]` 是指对于第 `i` 天，下一个更高温度出现在几天后。如果气温在这之后都不会升高，请在该位置用 `0` 来代替。
>
> **示例 1:**
>
> ```
> 输入: temperatures = [73,74,75,71,69,72,76,73]
> 输出: [1,1,4,2,1,1,0,0]
> ```
>
> **示例 2:**
>
> ```
> 输入: temperatures = [30,40,50,60]
> 输出: [1,1,1,0]
> ```
>
> **示例 3:**
>
> ```
> 输入: temperatures = [30,60,90]
> 输出: [1,1,0]
> ```





最朴素的想法：对于元素 $i$ ，遍历 $j \in [i+1, n)$ ，找到第一个 $a[j] > a[i]$ ，此时 $j - i$ 即为答案。找不到答案为 $0$ 。由此可以轻松写出代码：

```c++
class Solution {
public:
    vector<int> dailyTemperatures(vector<int>& a) {
        int n = a.size();
        vector<int> ans(n, 0);
        
        for (int i = 0; i < n; i++) {
            for (int j = i + 1; j < n; j++) {
                if (a[j] > a[i]) {
                    ans[i] = j - i;
                    break;
                }
            }
        }
    
        return ans;
    }
};
```



但是对于一个持续下行的序列：









元素 $6,5,4,3,2$ 都要遍历到 $7$ ，$5,4,3,2$ 均被多次遍历。存在重复。

上述思路中，元素有两个身份：

1. $[{\color{blue}6}, 5,4,3,2,7]$ 待寻找更大元素的 





